<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.zf.graduation">

    <application android:allowBackup="true" android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher" android:theme="@style/AppTheme">
    <activity android:name=".ui.MainActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent-filter>
    </activity>
    <activity android:name=".ui.ActivityActivity"></activity>
    <activity android:name=".ui.GroupActivity"></activity>
    <activity android:name=".ui.MessageActivity"></activity>
    <activity android:name=".ui.SetUpActivity"></activity>
    <activity android:name=".ui.CreateActivityActivity"></activity>
    <activity android:name=".ui.CreateGroupActivity"></activity>
    </application>

</manifest>
