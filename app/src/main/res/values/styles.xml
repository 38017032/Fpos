<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/ActionBar</item>
        <item name="actionBarStyle">@style/ActionBar</item>
        <item name="actionOverflowButtonStyle">@style/overflowButton</item>
    </style>
    <!--ActionBar-->
    <style name="ActionBar" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/bg</item>
        <item name="background">@color/bg</item>
        <item name="android:title">@string/test</item>
        <item name="title">@string/test</item>
    </style>
    <style name="overflowButton">
        <item name="android:src">@mipmap/ic_add_black_48dp</item>
        <item name="android:showAsAction"></item>
    </style>

    <style name="MaterialTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/MaterialActionBar</item>
    </style>
    <style name="MaterialActionBar" parent="android:Widget.ActionBar">
        <item name="android:background">@color/bg</item>
        <item name="android:icon">@android:color/transparent</item>
        <item name="android:titleTextStyle">@style/MaterialActionBar.TitleText</item>
    </style>
    <style name="MaterialActionBar.TitleText" parent="android:TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">18sp</item>
    </style>
</resources>
